.row
  .col-xs-12
    %h1= t(".title", season: CURRENT_SEASON)
.row
  - @games.each do |round, games|
    .col-xs-12.col-sm-4.col-md-4{class:  ("important" if round == CURRENT_ROUND)}
      = t("jornada", round: round)
      %table.table.statistic
        = render partial: "games/game", locals: {games: games}
